<h1>Flights#index</h1>
<p>Find me in app/views/flights/index.html.erb</p>

<% @flights.each do |f| %>
<%= "Depart : #{f.departure_airport} Arrivee : #{f.arrival_airport}" %>
<% end %>



<p>
</p>

<h2> form_tag </h2>
<%= form_tag  do -%>
  <div> <%= submit_tag 'Save' %></div>
<% end %>

<h2> submit search </h2>
<%= form_tag("/", method: :get) do %>
  <%= text_field_tag :mess %>

  <% if !@departure_airport %>
    <%= select(:departure_airport, @airport_options, selected: @departure_airport) %>
    <%= select(:arrival_airport, @airport_options, selected: @arrival_airport) %>
  <% else %>
    <%= select(:departure_airport, @airport_options) %>
    <%= select(:arrival_airport, @airport_options) %>
  <% end %>

  <%= submit_tag 'Search' %>
<% end %>
